services:
  - type: web
    name: echoes-world-server
    runtime: node
    rootDir: multiplayer-server
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /health
    autoDeploy: true
